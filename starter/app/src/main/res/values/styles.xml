<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/background</item>
        <item name="colorPrimaryDark">@color/background</item>
        <item name="colorAccent">@color/background_button</item>
    </style>
    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="dark_text_style">
        <item name="android:textColor">@color/text_color_dark</item>
        <item name="android:fontFamily">@font/tauri</item>
    </style>

    <style name="standard_style">
        <item name="android:textColor">@color/text_color_light</item>
        <item name="android:fontFamily">@font/tauri</item>
    </style>

    <style name="large_text_style" parent="standard_style">
        <item name="android:textSize">@dimen/font_large</item>
    </style>

    <style name="medium_text_style" parent="standard_style">
        <item name="android:textSize">@dimen/font_medium</item>
    </style>

    <style name="small_text_style" parent="standard_style">
        <item name="android:textSize">@dimen/font_small</item>
    </style>

</resources>
